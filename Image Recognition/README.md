# Image recognition

This files trains a neural network using the MNIST dataset from keras to be able to classify a picture into a set of 10 different classes.
In this file, two different callbacks are used as examples to treat the output data.
